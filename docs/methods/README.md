# methods

This directory contains method-oriented documentation for the MkDocs site. Pages
explain how `tcr-seek` stages FASTQs, merges multiple input FASTQs per
sample/read, runs the Snakemake workflow, computes diversity summaries, handles
optional rarefaction, and writes QC/MultiQC reports.
